{
  "name": "stripe-metrics-api",
  "version": "1.0.0",
  "private": true,

  "engines": { "node": "18.x" },

  "scripts": {
    "dev": "vercel dev"
  },

  "dependencies": {
    "stripe": "16.12.0"
  },

  "vercel": {
    "version": 2,
    "functions": {
      "api/**/*.js": { "runtime": "nodejs18.x" }
    }
  }
}
